syntax = "proto3";

package packet;

option go_package="github.com/RGood/go-grpc-udp/internal/generated/packet";

message MapEntry {
    string key = 1;
    repeated string values = 2;
}

message Packet {
    string id = 1;
    string method = 2;
    repeated MapEntry metadata = 3;
    bytes payload = 4;
}

service UDPServer {
    rpc Send(Packet) returns (Packet);
}
